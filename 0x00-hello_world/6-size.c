#include <stdio>
/**
 * main -  Entry Point
 * Discription: Program the print size of various types.
 * return 0 (success)
 */
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float f;
printf("size of a char %lu byte(s)\n", (unsigned long
sizeof(a)));
("size of an int %lu byte(s)\n", (unsigned long
sizeof(b)));
("size of an long int %lu byte(s)\n", (unsigned long
sizeof(c)));
("size of an long long int %lu byte(s)\n", (unsigned long
sizeof(d)));
("size of an float %lu byte(s)\n", (unsigned long
sizeof(f)));
return (0)
	;
}
